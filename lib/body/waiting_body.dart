import 'package:flutter/material.dart';

class WaitingBody extends StatelessWidget {
  const WaitingBody({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Welcome to the Waiting Screen!'),
    );  }
}
