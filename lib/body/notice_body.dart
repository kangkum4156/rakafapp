import 'package:flutter/material.dart';

class NoticeBody extends StatelessWidget {
  const NoticeBody({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Welcome to the Notice Screen!'),
    );  }
}
